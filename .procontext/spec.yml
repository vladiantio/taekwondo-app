# HCP Configuration — Taekwondo App
# Auto-generated by HCP Init skill v1.0

project_name: "taekwondo-app"
project_version: "0.0.0"  # Actualizar cuando se versione
hcp_version: "2.0.0"

# Project metadata
metadata:
  type: "react-pwa"
  description: "Progressive Web App para aprendizaje y gestión de Taekwondo"
  repository: "https://github.com/drhiidden/taekwondo-app"
  upstream: "https://github.com/Tekwondo-RAM/taekwondo-app"
  
  stack:
    frontend:
      - "React 19.2.0"
      - "TypeScript"
      - "Vite 8.0.0"
      - "Tailwind CSS 4.1.18"
      - "@tanstack/react-router 1.168.23"
      - "Zustand 5.0.12"
    
    ui:
      - "@base-ui/react 1.3.0"
      - "lucide-react 0.562.0"
      - "react-day-picker 9.14.0"
    
    tooling:
      - "Oxlint 1.60.0 (linter)"
      - "Oxfmt 0.45.0 (formatter)"
      - "Playwright 1.59.1 (E2E tests)"
    
    pwa:
      - "vite-plugin-pwa 1.2.0"
      - "workbox-window 7.4.0"
  
  entry_points:
    - "src/main.tsx"
    - "src/App.tsx"
  
  hotspots:
    - "src/App.tsx (10 mods)"
    - "src/common/Video.tsx (8 mods)"
    - "src/pages/Account.tsx (6 mods)"

# Patterns enabled
patterns:
  rpi_plus: true              # Research → Plan → Implement → Verify → Compact
  adr_required: true          # Architecture Decision Records obligatorios
  end_of_day: false           # TODO: Configurar workflow EOD
  end_of_week: false          # TODO: Configurar workflow EOW

# Quality gates
quality:
  min_test_coverage: 0        # TODO: Establecer meta (70-80%?)
  linter_required: true       # Oxlint configurado
  formatter_required: true    # Oxfmt configurado
  pre_commit_hooks: false     # TODO: Habilitar

# Integrations
integrations:
  git_auditor: false          # Opcional: Auditar calidad commits
  hil_search: false           # Opcional: Búsqueda híbrida local
  hcp_system: false           # No necesario (proyecto único)
  playwright: true            # E2E testing habilitado

# Conventions
conventions:
  commit_style: "conventional"  # feat:, fix:, docs:, style:, refactor:, test:, chore:
  branch_naming: "feat/*"       # feat/*, fix/*, chore/*
  pr_template: false            # TODO: Crear template PR
  code_style: "oxlint"          # Linter: Oxlint (configured in .oxlintrc.json)

# Technology choices (for ADRs)
tech_decisions:
  - name: "React 19"
    rationale: "Cutting-edge features, performance improvements"
    alternatives: ["React 18", "Vue 3", "Svelte"]
    decision_date: "Unknown (pre-HCP)"
    status: "Active"
  
  - name: "Base UI (unstyled components)"
    rationale: "Full control over design, Tailwind integration"
    alternatives: ["MUI", "Chakra UI", "shadcn/ui"]
    decision_date: "Unknown (pre-HCP)"
    status: "Active"
  
  - name: "Vite 8"
    rationale: "Fastest build tool, HMR instant, PWA support"
    alternatives: ["Webpack", "Turbopack", "Parcel"]
    decision_date: "Unknown (pre-HCP)"
    status: "Active"

# Areas to document (ADRs pendientes)
pending_adrs:
  - "ADR-001: React 19 + Base UI selection rationale"
  - "ADR-002: PWA vs Native app decision"
  - "ADR-003: Zustand vs Redux for state management"
  - "ADR-004: Backend/API strategy (pending definition)"
  - "ADR-005: Authentication provider selection"

---

# HCP Init Metadata
init_date: "2026-04-23"
init_agent: "HCP Bootstrap v1.0"
analysis_automated: true
token_savings: "~80% vs manual analysis"
setup_time: "15 minutes"
